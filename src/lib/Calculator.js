class Calculator {
  static add(x, y) {
    return x + y;
  }

  static subtract(x, y) {
    return x - y;
  }

  static division(x, y) {
    return x / y;
  }

  static multiply(x, y) {
    return x * y;
  }

  static mod(x, y) {
    return x % y;
  }
}

export default Calculator;
